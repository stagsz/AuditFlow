# Archive

Files kept for reference but no longer used by the app or its build/deploy pipeline.
Moved here (via `git mv`, so history is preserved) rather than deleted.

- `railway.json` — old Railway deploy config; deploys now run on Vercel (`vercel.json`).
- `MinAuditFlow.json` — one-off GitHub branch-ruleset export; not referenced by anything.
- `landing/` — leftover screenshot from the old standalone landing page, which was
  ported into `frontend/src/components/landing/`.
